# Release assets

`RELEASE-ASSET-PARTS.json` records the five verified parts prepared for an optional GitHub Release mirror.

The binary parts are deliberately excluded from Git history. Upload them only to a draft Release after the release identifier, metadata, authorship, licence, and publication decision are approved. Verify every uploaded asset against this manifest before making a release public.

